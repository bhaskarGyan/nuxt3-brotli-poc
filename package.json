{
  "private": true,
  "scripts": {
    "build": "npx nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.8",
    "push-dir": "^0.4.1"
  },
  "dependencies": {
    "vite-plugin-compression": "^0.5.1"
  }
}
